import { Menu, X } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import BrushButton from './BrushButton'
import logo from '../assets/logo/purple-cow-logo-new.jpg'
const links=[{label:'Home',to:'/'},{label:'About Us',to:'/about'},{label:'Services',to:'/services'},{label:'Insights',to:'/insights'},{label:'Contact',to:'/contact'}]
export default function Navbar(){const[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(scrollY>14);onScroll();addEventListener('scroll',onScroll,{passive:true});return()=>removeEventListener('scroll',onScroll)},[]);return <header className={`site-header ${scrolled?'is-scrolled':''}`}><nav className="navbar" aria-label="Main navigation"><Link to="/" className="brand" aria-label="We Are Purple Cow home"><img src={logo} alt="We Are Purple Cow"/></Link><div className={`nav-panel ${open?'is-open':''}`}><div className="nav-links">{links.map(link=><Link to={link.to} key={link.label} onClick={()=>setOpen(false)}>{link.label}</Link>)}</div><div className="mobile-talk"><BrushButton compact>Let&apos;s talk</BrushButton></div></div><div className="desktop-talk"><BrushButton compact>Let&apos;s talk</BrushButton></div><button className="menu-toggle" type="button" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button></nav></header>}