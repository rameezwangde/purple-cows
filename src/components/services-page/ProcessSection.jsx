import { processSteps } from '../../data/processSteps'
export default function ProcessSection(){return <section className="sp-process" aria-labelledby="process-title"><div className="sp-process-heading sp-reveal"><p className="sp-eyebrow">Our process</p><h2 id="process-title">A simple process. <span>Powerful</span> results.</h2></div><div className="sp-steps">{processSteps.map(({number,title,copy,icon:Icon},index)=><article className="sp-step sp-reveal" key={number}><Icon aria-hidden="true"/><strong>{number}</strong><h3>{title}</h3><p>{copy}</p>{index<processSteps.length-1&&<i aria-hidden="true">→</i>}</article>)}</div></section>}